node_modules
dist
.env
.env.local

# Next.js (regenerated each build)
.next
next-env.d.ts

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# GitLab Runner (shell executor) working directory
builds/
